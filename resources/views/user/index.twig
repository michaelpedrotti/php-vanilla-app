{% extends "layout/document.twig" %}
{% block content %}
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Name</th>
            <th>E-mail</th>
            <th>Actions</th>
        </tr>  
    </thead>
    <tbody>
        {% if rows is empty %}
            <tr>
                <td colspan="3">Nenhum resultado foi encontrado</td>
            </tr>   
        {% else %}
            {% for row in rows %} 
                <tr>
                    <td>{{ row.name }}</td>
                    <td>{{ row.email }}</td>
                    <td style="width: 10rem; text-align:center">
                        <a href="" class="btn-link">Edit</a>
                        <a href="javascript:void(0)" class="btn-link">Delete</a>
                    </td>
                </tr>
            {% endfor %}
        {% endif %}
    </tbody>
    <tfoot>
        0 of 10 from {{ total }}  
    </tfoot>
</table>
{% endblock %}