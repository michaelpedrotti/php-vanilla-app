{% extends "layout/document.twig" %}
{% block content %}
    <form>
      <div class="form-group">
        <label>Name</label>
        <div class="form-control">{{ data.name }}</div>
      </div>
      <div class="form-group">
        <label>Hex</label>
        <div class="form-control">{{ data.hex }}</div>
      </div>     
      <a href="/color" class="btn btn-link btn-secondary">Back</a>
    </form>
{% endblock %}